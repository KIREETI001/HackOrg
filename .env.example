# Only the Python scanner needs a key. In GitHub Actions, configure it as
# the OPENAI_API_KEY repository secret. Never expose it as NEXT_PUBLIC_*.
OPENAI_API_KEY=

# The website reads public scan data and links to GitHub for manual scans.
# No GITHUB_TOKEN or OpenAI key is required in Vercel.
